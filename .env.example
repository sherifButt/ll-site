# NextJS Environment Variables

# Base URL of the site (used for revalidation)
NEXT_PUBLIC_BASE_URL=https://your-site.com

# API key for content API authentication
CONTENT_API_KEY=your-secure-api-key

# Git operations (set to "true" to enable git operations)
ENABLE_GIT_OPERATIONS=false

# Git user configuration (required if ENABLE_GIT_OPERATIONS is true)
GIT_USER_NAME=your-git-username
GIT_USER_EMAIL=your-git-email

# Git personal access token (recommended for secure authentication)
GIT_ACCESS_TOKEN=your-personal-access-token

# Other environment variables for the site
# Add any other environment variables your site needs below
